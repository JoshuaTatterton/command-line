ps follows the following format "ps -_"

if _ is left blank then it will display all the processes being run by the terminal.

_ can be multiple letters to change what processes are displayed
e.g. if _ is x then all processes being run by the computer are displayed
