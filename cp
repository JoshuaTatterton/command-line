cp functions in the format "cp x y"

where x is a file/file location

if y is a directory/directory location then file x will be copied to that location
if y is not the above then x will be copied (and overwrite if file y already exists) to file name y 
